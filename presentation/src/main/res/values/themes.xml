<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Dokusha" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <!-- Splash Screen theme. -->
    <style name="Theme.Dokusha.SplashScreen" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Theme.Dokusha</item>
    </style>
    <!-- Base application theme. -->
    <style name="Theme.Dokusha" parent="Base.Theme.Dokusha" />
</resources>